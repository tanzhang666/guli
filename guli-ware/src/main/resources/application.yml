spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.33.10:3306/gulimall_wms
    username: root
    password: root
  application:
    name: guli-ware
  cloud:
    nacos:
      discovery:
        server-addr: localhsot:8848
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 11000